<%- include('../partials/header') %>

<div class="container d-flex flex-column align-items-center mt-5">
    <img src="<%= viewData.image %>" 
         alt="Pokenea" 
         width="300" height="300" 
         class="rounded-circle border border-4 border-warning shadow"/>
    <h2 class="mt-3">Frase del día</h2>
    <p class="fst-italic text-center fs-5">"<%= viewData.quote %>"</p>
    <div class="badge bg-secondary mt-2 fs-6">
        Container ID: <%= viewData.hostname %>
    </div>
</div>

<%- include('../partials/footer') %>